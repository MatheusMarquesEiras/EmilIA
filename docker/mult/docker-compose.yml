services:
  ollama-1:
    image: ollama/ollama:latest
    container_name: ollama-1
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ../../storage/backup-1:/root/.ollama
      - ../../model:/root/context
    
    environment:
      - OLLAMA_MAX_LOADED_MODELS=1        
      - OLLAMA_NUM_PARALLEL=1
      - OLLAMA_FLASH_ATTENTION=1
      - OLLAMA_KEEP_ALIVE=30m             
      - OLLAMA_MAX_QUEUE=512              
      - OLLAMA_MODELS=/root/.ollama/models 
      - CUDA_VISIBLE_DEVICES=0
    
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
  # ollama-2:
  #   image: ollama/ollama:latest
  #   container_name: ollama-2
  #   restart: unless-stopped
  #   ports:
  #     - "11435:11434" # Mapeado na porta externa 11435
  #   volumes:
  #     - ../../storage/backup-2:/root/.ollama
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: all
  #             capabilities: [gpu]